# Inference Folder

Contains trained ML models for offline use:

- `emergency_classifier.pkl` – Trained RandomForest classifier
- `scaler.pkl` – StandardScaler for preprocessing input
- `emergency_classifier.py` – Helper script to make predictions offline
